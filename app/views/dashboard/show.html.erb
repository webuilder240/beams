<% content_for :title, "ダッシュボード" %>

<div class="space-y-4">
  <h1 class="text-2xl font-bold">ダッシュボード</h1>
  <p>ようこそ、<%= current_user.email %> さん。</p>

  <% if current_user.admin? %>
    <p>
      <%= link_to "ユーザー管理", admin_users_path, class: "text-blue-600 hover:underline" %>
    </p>
  <% end %>
</div>
