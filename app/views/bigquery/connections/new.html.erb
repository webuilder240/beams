<% content_for :title, "新規 BigQuery 接続" %>

<div class="max-w-xl mx-auto space-y-6">
  <h1 class="text-2xl font-bold">新規 BigQuery 接続</h1>

  <%= render "form",
        connection: @connection,
        url: bigquery_connections_path,
        method: :post,
        submit_label: "作成",
        persisted: false %>
</div>
