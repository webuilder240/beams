<%#
  ウィジェット一覧（Turbo Frame `widgets`）を丸ごと差し替える共通ストリーム。
  create / destroy / move_up / move_down の各アクションから再利用する。
%>
<%= turbo_stream.replace "widgets" do %>
  <%= turbo_frame_tag "widgets" do %>
    <%= render "widgets/widgets", dashboard: @dashboard, widgets: @widgets %>
  <% end %>
<% end %>
